---
alwaysApply: true
---

This app is intentionally pull-based with no realtime subscriptions.

## Rules
- Never use Supabase realtime (no `supabase.channel()`, no websocket listeners, no `on('postgres_changes', ...)`)
- Never implement optimistic UI updates
- Never push data to the client — all data is fetched on demand
- UI refreshes on navigation or explicit user action (e.g. pull-to-refresh) only
- All time-sensitive logic (e.g. cutoff scoring) runs server-side on a schedule, not triggered by client actions

## Rationale
- Submission model is once-per-day; realtime updates provide no meaningful UX benefit
- Leaderboard results are intentionally hidden until cutoff or full submission — push updates would undermine this
- Keeps client logic simple and the architecture easy to reason about
- Explicitly agreed upon with the client as a design requirement

## Acceptable patterns
- `supabase.from(...).select(...)` queries on component mount or user-triggered refresh
- Server-side scheduled functions (e.g. Supabase Edge Functions via cron) for cutoff processing
- Returning fresh data after a mutation by re-fetching, not by updating local state directly
- pull-to-refresh via RefreshControl is the preferred explicit refresh mechanism for list screens.


## Development
- Any debug logging or dev-only UI should be gated behind __DEV__ checks. Create a utils/logger.ts with a devLog utility and use that instead of raw console.log throughout the app.
- The app targets iOS and Android as its primary platforms. Web (via Expo Web) is supported for development and testing purposes only. Prioritize React Native layout and UX patterns. Do not add web-specific features or optimize for web-only behavior.
- When needing to make db migrations, do not modify existing migration files, instead create a new migration file. Never edit a migration file after initial creation. If changes are needed, create a new migration file instead."

## Code Style: Early Returns and Flat Functions

Async data loading functions must use early returns for all error and edge 
cases. The happy path should flow linearly down the function without nested 
conditionals.
- Handle all failure and edge cases at the top of the function with an 
  immediate return
- Never re-check a condition that has already been handled by an earlier return
- Avoid nested if/else blocks in async functions — flatten them with returns
- By the time the happy path code runs, all preconditions should be guaranteed 
  without further checks

# Database: PL/pgSQL Function Syntax

When writing PL/pgSQL functions in migrations, follow these rules:

## Rules
- Never use CTEs (`WITH` blocks) combined with `PERFORM` — this is invalid 
  PL/pgSQL syntax
- Never nest `DECLARE/BEGIN` blocks inside a function body — there is one 
  `DECLARE` block at the top of the function only
- Use `FOR ... LOOP` for iterating over query results, not CTEs with PERFORM
- Use `PERFORM` only for calling functions where the return value is discarded
- Test all PL/pgSQL syntax mentally against PL/pgSQL rules, not plain SQL rules 
  — they are different languages with different constraints